import 'package:flutter/material.dart';
class SeguimientoHumedadPage extends StatelessWidget {
  const SeguimientoHumedadPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Seguimiento de Humedad'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción')));
  }
}