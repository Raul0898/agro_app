import 'package:flutter/material.dart';
class InstalacionEquiposMedicionPage extends StatelessWidget {
  const InstalacionEquiposMedicionPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Instalación de Equipos de Medición'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción')));
  }
}