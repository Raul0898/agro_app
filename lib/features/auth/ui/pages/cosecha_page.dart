import 'package:flutter/material.dart';
class CosechaPage extends StatelessWidget {
  const CosechaPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Cosecha'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción')));
  }
}