import 'package:flutter/material.dart';
class AnalisisMalezasPage extends StatelessWidget {
  const AnalisisMalezasPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Análisis de Malezas'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción')));
  }
}