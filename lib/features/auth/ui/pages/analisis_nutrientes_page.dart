import 'package:flutter/material.dart';
class AnalisisNutrientesPage extends StatelessWidget {
  const AnalisisNutrientesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Análisis de Nutrientes'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción rrrrrrr')));
  }
}