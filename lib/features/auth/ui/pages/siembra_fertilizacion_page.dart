import 'package:flutter/material.dart';
class SiembraFertilizacionPage extends StatelessWidget {
  const SiembraFertilizacionPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Siembra y Fertilización'), backgroundColor: const Color(0xFFF2AE2E)), body: const Center(child: Text('Pantalla en construcción')));
  }
}