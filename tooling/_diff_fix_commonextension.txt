commit ac4491559b4d6d80008e1393a3b7994d2d785a3a
Author: Codex <codex@openai.com>
Date:   Thu Oct 30 02:59:07 2025 +0000

    fix: clean android root gradle configuration

diff --git a/android/build.gradle.kts b/android/build.gradle.kts
index 0f04d55..992fc4b 100644
--- a/android/build.gradle.kts
+++ b/android/build.gradle.kts
@@ -1,8 +1,3 @@
-import com.android.build.api.dsl.CommonExtension
-import org.gradle.api.plugins.JavaPluginExtension
-import org.jetbrains.kotlin.gradle.dsl.JvmTarget
-import org.jetbrains.kotlin.gradle.tasks.KotlinJvmCompile
-
 plugins {
     java
 }
@@ -23,23 +18,6 @@ rootProject.layout.buildDirectory.value(newBuildDir)
 subprojects {
     val newSubprojectBuildDir: Directory = newBuildDir.dir(project.name)
     project.layout.buildDirectory.value(newSubprojectBuildDir)
-
-    extensions.findByName("android")?.let { extension ->
-        if (extension is CommonExtension<*, *, *, *, *>) {
-            extension.compileOptions {
-                sourceCompatibility = JavaVersion.VERSION_17
-                targetCompatibility = JavaVersion.VERSION_17
-            }
-        }
-    }
-
-    tasks.withType<KotlinJvmCompile>().configureEach {
-        compilerOptions.jvmTarget.set(JvmTarget.JVM_17)
-    }
-
-    extensions.findByType(JavaPluginExtension::class.java)?.toolchain?.languageVersion?.set(
-        JavaLanguageVersion.of(17)
-    )
 }
 subprojects {
     project.evaluationDependsOn(":app")
